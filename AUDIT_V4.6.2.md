# تقرير تدقيق زاد المسلم v4.6.2

تاريخ التدقيق: 15 سبتمبر 2026

## الإصلاحات المنفذة

1. **منع تداخل الصوت:** كل مشغل يوقف الآخر عند بدء التشغيل أو استئنافه، وليس عند اختيار ملف جديد فقط.
2. **تتبع الآية:** بعد نجاح تشغيل كل آية تُحدَّث العلامة النشطة وتُوسَّط الآية في الشاشة. يحترم التمرير إعداد تقليل الحركة في الجهاز.
3. **تنزيل قالون:** الاستجابة المعتمة لا تُعامل وحدها كدليل نجاح؛ يُختبر الرابط بعنصر صوت حتى يصل إلى بيانات الملف، ثم يُحفظ. عند الفشل يُحذف الملف وإدخاله الوصفي من الكاش.
4. **آخر سورة:** اختيار قارئ من مكتبة المصاحف الكاملة يفتح آخر سورة صوتية محفوظة بدل القفز دائمًا إلى الفاتحة.
5. **تنسيق الوقت:** خيار 12/24 ساعة في إعدادات الصلاة. تبقى الأوقات الداخلية بصيغة 24 ساعة للحساب الصحيح، ويتغير العرض فقط.
6. **تخزين الموقع:** أزيلت الكتابة والحذف الاحتياطيان من `index.html`. أصبحت لوحة الموقع تستدعي الدوال المركزية في `app.js` فقط.
7. **أسماء الملفات:** استبدلت أسماء الملفات المرتبطة بالإصدارات بأسماء ثابتة واضحة، وحُدّثت جميع مراجع HTML وService Worker.
8. **التحميل:** نُقلت ملفات JavaScript الخارجية إلى رأس الصفحة مع `defer` مع الحفاظ على ترتيب الاعتماديات.
9. **الإصدار المركزي:** أضيف `config.js` ليكون مرجع رقم الإصدار البرمجي داخل الواجهة والتصدير.

## ملاحظة مهمة حول التتبع

التتبع الدقيق متاح لمشغل الآيات الذي يملك ملفًا مستقلًا لكل آية. أما المصاحف الصوتية الكاملة، ومنها تسجيلات قالون الحالية، فهي ملف واحد للسورة ولا تتضمن توقيت بداية ونهاية كل آية؛ لذلك لا يدّعي التطبيق مزامنتها مع نص حفص. تحتاج مزامنة قالون إلى نص قالون مرخّص وملفات توقيت مدققة لكل قارئ وسورة.

## اختبارات القبول

- فحص بناء جميع ملفات JavaScript.
- التحقق من صحة جميع ملفات JSON.
- التأكد من عدم وجود مراجع إلى أسماء ملفات الإصدار القديمة في HTML أو Service Worker.
- التأكد من أن كل ملف في قائمة Service Worker موجود فعليًا.
- التحقق من وجود 114 سورة و6236 آية وثبات بصمة ملف القرآن.
- فحص بنية ملف ZIP بعد إنشائه.

# تقرير تدقيق زاد المسلم v4.6.1

تاريخ التدقيق: 15 سبتمبر 2026

## نطاق الإصدار

- إضافة مشغل مستقل للمصاحف الصوتية الكاملة (سورة كاملة)، من دون تعديل ملف نص القرآن.
- إضافة 10 إصدارات صوتية ليبية تمثل 7 قراء، منها 6 مصاحف برواية قالون، و4 روايات للشيخ مفتاح السلطني.
- إضافة المصاحف الكاملة المطلوبة: محمد اللحيدان، ياسر الدوسري، وبدر التركي.
- إضافة رابط مباشر إلى **مصحف الأوقاف الليبية الإلكتروني برواية قالون عن نافع**.
- دعم التشغيل، السابق/التالي، استكمال موضع السماع، السحب داخل المقطع، وتنزيل السورة إلى Cache Storage للاستماع دون اتصال عندما يسمح المصدر والمتصفح بذلك.

## الفصل بين الروايات

نص القرآن المضمّن في التطبيق هو نص عثماني برواية حفص، وقد بقي بلا تعديل. لذلك:

- لا تتم مزامنة أو تظليل آيات تسجيلات قالون فوق نص حفص.
- يظهر اسم الرواية بوضوح قبل التشغيل.
- يظهر تنبيه عند اختيار تسجيل غير حفص.
- مصحف قالون الرسمي يُفتح في قسم مستقل عبر موقع الأوقاف الليبية، ولم تُنسخ صفحاته أو بياناته داخل التطبيق لعدم وجود إذن إعادة استخدام أو واجهة بيانات معلنة تسمح بذلك.

## التحقق من المصادر

- فهرس MP3Quran الرسمي أظهر قائمة `001..114` للمصاحف المدرجة منه.
- صفحة عبد الحميد القريو في Qaloun.app تعرض مصحفًا كاملًا، وتم التحقق بعينة من أول سورة وآخر سورة على خادم الصوت.
- تم فحص رابط السورة الأولى والأخيرة لكل مصدر مضاف، وكانت الاستجابة ملف `audio/mpeg` ناجحًا وقت التدقيق.
- خادم Qaloun.app يقيّد CORS؛ لذلك يستخدم التنزيل طلبًا غير مقروء `no-cors` ويحفظ الاستجابة المعتمة مباشرة في Cache Storage. نجاح ذلك قد يختلف حسب المتصفح وسياسة الخادم.
- لا تُخزّن ملفات الصوت ضمن حزمة التطبيق؛ التشغيل والتنزيل يعتمدان على توفر المصدر الخارجي.

## نتائج فحص الحزمة

- جميع ملفات JavaScript اجتازت `node --check`.
- جميع ملفات JSON صالحة للقراءة.
- فهرس المصاحف يحتوي 13 إصدارًا: 10 ليبية و3 مطلوبة.
- جميع إدخالات الفهرس معرفة بأنها 114 سورة وبروابط HTTPS.
- ملف القرآن بقي 114 سورة و6236 آية، وبصمة SHA-256 لم تتغير عن v4.6.0.
- Service Worker يضم ملفات الإصدار الجديدة ويحافظ على مخزني الصوت عند التحديث.
- حذف بيانات التطبيق لا يستخدم `localStorage.clear()` ويحذف مفاتيح زاد المسلم فقط.

## حدود يجب معرفتها

- وصف «كامل» مبني على فهرس المصدر الذي يصرّح بوجود 114 سورة، مع اختبار عينات الروابط؛ لم تُنزّل الملفات الصوتية الـ114 لكل قارئ لأن حجمها كبير جدًا.
- حقوق التسجيلات وشروط إعادة التوزيع تعود لأصحاب المصادر. التطبيق يربط بالمصادر ولا يعيد تضمين ملفات MP3 داخل الحزمة.
- يجب اختبار التشغيل في الخلفية والتنزيل والبوصلة على جهاز Android حقيقي، لأن سياسات الوسائط والحساسات تختلف بين المتصفحات والأجهزة.

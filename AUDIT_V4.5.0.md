# تقرير تدقيق زاد المسلم v4.5.0

تاريخ التدقيق: 15 سبتمبر 2026

## نطاق الإصدار

- تلاوة مستقلة لكل آية وتشغيل متتابع للسورة.
- خمسة قراء، وتكرار الآية، وسرعات تشغيل متعددة.
- حفظ موضع الاستماع وإبرازه داخل المصحف.
- تنزيل السورة للقارئ المحدد إلى Cache Storage وحذفها منفردة أو مجتمعة.
- دعم Media Session في الأجهزة المتوافقة.

## نتائج التحقق

- ملفات JavaScript: اجتاز جميعها فحص `node --check`.
- manifest: ملف JSON صالح وإصدار التطبيق 4.5.0.
- المصحف: 114 سورة و6236 آية.
- بصمة `quran-data/uthmani.json`: `adaebb377c60eba1bab6ef652959c7a0b4ccb4d0ca42145945c14ecdbeb4b761`، مطابقة تمامًا للنسخة المدققة v4.4.1.
- فهارس المصحف: 30 جزءًا، 60 حزبًا، 240 ربعًا، 604 صفحات، و15 موضع سجدة.
- ملفات التلاوة: اختُبرت عينة البداية `001001`، ونهاية البقرة `002286`، ونهاية الناس `114006` للقراء الخمسة؛ أعاد المصدر HTTP 200 ونوع `audio/mpeg`.
- CORS: المصدر يعيد `Access-Control-Allow-Origin: *`، وهو لازم للتنزيل إلى Cache Storage.
- PWA: أضيف `quran-audio-v45.js` إلى كاش التطبيق، ورُفع اسم الكاش إلى `zad-al-muslim-v4-5-0`.
- اختبار HTTP محلي: عادت الصفحة الرئيسية وملفات CSS وJavaScript والمصحف والفهرس وmanifest وService Worker وصفحة Offline كلها بحالة 200.
- حماية التنزيلات: Service Worker يحتفظ بكاش الصوت `zad-quran-audio-v45` عند تحديث ملفات التطبيق.
- النسخ الاحتياطي: تُصدّر إعدادات الصوت وموضع الاستماع، ولا تُصدّر فهارس التنزيل الوهمية لأن الملفات الكبيرة نفسها لا تدخل JSON.
- حذف البيانات: يحذف مفاتيح التطبيق وكاش التلاوات فقط، دون استخدام `localStorage.clear()`.

## ضوابط التشغيل

- لا يحتوي ZIP على تلاوات؛ تُجلب عند الطلب لتقليل الحجم.
- التشغيل المباشر يحتاج اتصالًا، أما السورة المنزلة فتعمل دون اتصال على الجهاز نفسه.
- تنزيل سورة طويلة قد يستهلك مساحة ملحوظة، ويعرض التطبيق تقدير مساحة التخزين قبل/بعد التنزيل.
- قد يمسح نظام الهاتف Cache Storage تلقائيًا عند ضيق المساحة؛ تتحقق الواجهة من أول وآخر ملف ولا تعرض علامة «محفوظة» إذا كان التنزيل ناقصًا.
- تنزيلات الصوت خاصة بالجهاز ولا تنتقل مع ملف النسخة الاحتياطية.

## المصدر

ملفات التلاوة من EveryAyah، مع تسجيل المصدر في `THIRD_PARTY_ASSETS.txt`. نص المصحف من Tanzil ولم يُعدّل.

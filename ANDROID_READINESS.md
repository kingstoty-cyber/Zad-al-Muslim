# تجهيز زاد المسلم كتطبيق Android

الإصدار v4.8 يضيف هيكل Capacitor فقط، ولا يدّعي أن APK موقّع قد بُني. تبقى نسخة GitHub Pages هي المصدر الأساسي، وتُنسخ ملفات الويب المسموح بها إلى `www` عند كل مزامنة.

## البدء

1. ثبّت Node.js LTS وAndroid Studio وJDK الموافق لإصدار Android Gradle Plugin.
2. نفّذ `npm install`.
3. لأول مرة فقط: `npm run android:add`.
4. بعد كل تعديل: `npm run android:sync`.
5. افتح المشروع: `npm run android:open`.
6. اختبر على هاتف حقيقي قبل إنشاء النسخة الموقعة.

## اختبارات إلزامية قبل النشر

- الموقع اليدوي وGPS ومواقيت الصلاة والقبلة والبوصلة على هاتف حقيقي.
- تشغيل الصوت في الخلفية، أزرار الوسائط، التوقف عند المكالمة، والتنزيل دون اتصال.
- زر الرجوع، الروابط العميقة، الوضع الداكن، تكبير الخط، وقارئ الشاشة.
- التثبيت النظيف، التحديث مع الاحتفاظ بالبيانات، وتصدير/استعادة النسخة الاحتياطية.
- مراجعة أذونات الموقع والإشعارات والتخزين، وسياسة الخصوصية ونموذج أمان البيانات في المتجر.

لا توضع مفاتيح التوقيع أو كلمات المرور داخل المستودع. أنشئ ملف توقيع خارج المشروع واحفظ نسخة آمنة منه، لأن فقدانه قد يمنع تحديث التطبيق مستقبلًا.

## المزامنة بين الأجهزة

v4.8 يحفظ القوائم والعلامات والإعدادات ضمن ملف النسخة الاحتياطية المحلي. المزامنة السحابية تحتاج حساب مستخدم، تشفيرًا أثناء النقل والتخزين، سياسة خصوصية، وآلية حل التعارض؛ لذلك لا تُفعّل قبل اختيار خدمة خلفية موثوقة ومراجعة أمنية مستقلة.
